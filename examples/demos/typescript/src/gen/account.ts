// Code generated by json-schema-to-typescript from the kind manifest. DO NOT EDIT.

export interface AccountSpec {
  /**
   * Root-account contact email.
   */
  email: string;
  /**
   * Owner team name; appears in tags.
   */
  team_name: string;
}
export interface AccountStatus {
  /**
   * Provider-issued account id, formatted from the config's id_pattern.
   */
  account_id: string;
}
/**
 * The account provider's config: the name pattern it needs before it can mint an id. The provider's readiness is gated on this — no config, no work.
 */
export interface AccountConfig {
  /**
   * Account-id template; `{team_name}` is substituted at work time, e.g. "acc-{team_name}-prod".
   */
  id_pattern: string;
}
